# mujoco_env package
# This file makes the mujoco_env directory a Python package

# Instead of immediately importing everything, we'll import modules only when needed
# This prevents import errors during module discovery