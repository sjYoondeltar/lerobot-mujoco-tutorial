dataset:
  repo_id: omy_pnp_language # Repository ID
  root: ./merged_omy_language_data # Your root for data file!

policy:
  type: smolvla
  chunk_size: 5
  n_action_steps: 5
  device: cuda
  push_to_hub: false
  load_vlm_weights: true
  use_amp: false  # Disabled for BFloat16 compatibility (auto-detected)
  
optimizer:
  type: adamw
  lr: 3e-4
  weight_decay: 0.01
  grad_clip_norm: 1.0
  betas: [0.9, 0.999]

save_checkpoint: true
output_dir: ./ckpt/smolvla_omy_multigpu # Save directory
batch_size: 256  # Total batch size across all GPUs (64 per GPU on 4 GPUs)
job_name: smolvla_omy_multigpu
resume: false 
seed: 42  # Fixed seed for consistent model initialization across processes
num_workers: 16  # More workers for multi-GPU (4 per GPU)
steps: 20_000
eval_freq: -1 # No evaluation
log_freq: 50
save_checkpoint: true
save_freq: 1_000
use_policy_training_preset: true

wandb:
  enable: false
  project: smolvla_omy_multigpu
  entity: <your_wandb_entity>
  disable_artifact: true 